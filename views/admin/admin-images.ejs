<div class="tab-pane fade" id="admin-images">
    <div class="row pb-3">
        <div class="col-12">
            <% images.forEach(image => { %>
                <div class="row py-2">
                    <div class="col-2">
                        <img width="100" height="100" src="<%= image.url %>" alt="<%= image.alt %>">
                        <p><%= image.title %></p>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</div>