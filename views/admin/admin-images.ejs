<div class="tab-pane fade" id="admin-images">
    <div class="row pb-3">
        <div class="col-12">
            <div class="row py-2">
                <% images.forEach(image => { %>
                    <div class="col-2">
                        <img style="width: 100px; height: 125px; object-fit: cover;" src="<%= image.url %>" alt="<%= image.alt %>">
                        <p><%= image.title %></p>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</div>